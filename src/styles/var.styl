@import 'fn'
//stylus var
primary = rgb(97, 50, 239);
warning = #faad14;
danger = #ff4d4f;
gray = #dadada;

/* css vars */
:root{
  colorGroup(--primary,primary)
  colorGroup(--warning,warning)
  colorGroup(--danger,danger)
  colorGroup(--gray,gray)
  --radius-val:2px;
}